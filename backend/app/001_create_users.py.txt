"""create users table

Revision ID: 001_create_users
Revises:
Create Date: 2026-05-26

This is the first migration. It creates the users table with all fields
defined in app/models.py.

Apply with:   alembic upgrade head
Roll back:    alembic downgrade -1
"""

from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects.postgresql import UUID


# Alembic revision identifiers
revision = "001_create_users"
down_revision = None          # None means this is the first migration
branch_labels = None
depends_on = None


def upgrade() -> None:
    """
    Create the users table.

    The UUID primary key uses PostgreSQL's gen_random_uuid() function
    so the database generates the ID — not Python.
    """
    op.create_table(
        "users",

        # Primary key — UUID generated by PostgreSQL
        sa.Column(
            "id",
            UUID(as_uuid=True),
            primary_key=True,
            server_default=sa.text("gen_random_uuid()"),
            nullable=False,
        ),

        # Identity
        sa.Column("email",           sa.String(320),  nullable=False),
        sa.Column("hashed_password", sa.String(72),   nullable=False),

        # Brand profile (set during onboarding, not at registration)
        sa.Column("target_brand_name",  sa.String(255), nullable=True),
        sa.Column("target_brand_lat",   sa.Float(),     nullable=True),
        sa.Column("target_brand_long",  sa.Float(),     nullable=True),

        # Subscription
        sa.Column(
            "subscription_status",
            sa.String(32),
            nullable=False,
            server_default="free_tier",
        ),
        sa.Column("trial_ends_at",      sa.DateTime(timezone=True), nullable=True),
        sa.Column("stripe_customer_id", sa.String(64),              nullable=True),
        sa.Column(
            "is_verified",
            sa.Boolean(),
            nullable=False,
            server_default="false",
        ),

        # Timestamps
        sa.Column(
            "created_at",
            sa.DateTime(timezone=True),
            nullable=False,
            server_default=sa.text("NOW()"),
        ),
        sa.Column(
            "updated_at",
            sa.DateTime(timezone=True),
            nullable=False,
            server_default=sa.text("NOW()"),
        ),
    )

    # ── Indexes ───────────────────────────────────────────────────────────────
    # Unique index on email — enforces one account per email address
    # and makes login lookups fast.
    op.create_index(
        index_name="ix_users_email",
        table_name="users",
        columns=["email"],
        unique=True,
    )

    # Unique index on stripe_customer_id — ensures each Stripe customer
    # maps to exactly one ReviewSnipper user.
    op.create_index(
        index_name="ix_users_stripe_customer_id",
        table_name="users",
        columns=["stripe_customer_id"],
        unique=True,
    )


def downgrade() -> None:
    """
    Drop the users table and all its indexes.

    WARNING: This permanently deletes all user data.
    Only use during development — never run downgrade in production.
    """
    op.drop_index("ix_users_stripe_customer_id", table_name="users")
    op.drop_index("ix_users_email",              table_name="users")
    op.drop_table("users")
